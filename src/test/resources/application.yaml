spring:
  profiles:
    active: test

application:
  stream-sampler:
    bootstrap-servers: 127.0.0.1:9092
    inbound-topic: characters-integration-test
    application-id: kafka-streams-sampler-integration-test
    client-id: kafka-streams-sampler-client-integration-test

redis:
  addresses: "redis://127.0.0.1:6379"

